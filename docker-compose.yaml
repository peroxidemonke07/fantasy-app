version: '3.8'

services:
  frontend:
    image: fantasy-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    image: fantasy-backend
    ports:
      - "5000:5000"
    environment:
      MONGO_URL: "mongodb+srv://mayank:njr10psg@cluster0.vxdhx.mongodb.net/fantasy_app?retryWrites=true&w=majority&appName=Cluster0"  
      KEDRO_API_URL: "http://kedro-api:5008"
    depends_on:
      - kedro-api

  kedro-api:
    image: fantasy-kedro-api
    ports:
      - "5008:5008"
